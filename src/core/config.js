// 配置定义

export const APP_DEFAULTS = {
  // 应用元数据
  name: "My App",
  shortName: "App",
  
  // 颜色配置
  brand: "#0071e3",    // 品牌色 (Azure Blue)
  bg: "#444546",       // 背景色 (Cast Iron / 铸铁灰)
  fg: "#ffffff",       // 前景色 (Pure White / 纯白)
  
  // 生成参数
  goldenRatio: 0.65,   // Maskable 图标的安全区缩放比例
};